import { dsbClientGatewayErrors } from './dsb-client-gateway-errors';

describe('dsbClientGatewayErrors', () => {
  it('should work', () => {
    expect(dsbClientGatewayErrors()).toEqual('dsb-client-gateway-errors');
  });
});
