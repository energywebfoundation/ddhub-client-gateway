export enum RoleStatus {
  approved = 'APPROVED',
  pending = 'PENDING',
  requested = 'REQUESTED',
  rejected = 'REJECTED',
  synced = 'SYNCED',
}

export enum ExpirationStatus {
  expired = 'EXPIRED',
}
