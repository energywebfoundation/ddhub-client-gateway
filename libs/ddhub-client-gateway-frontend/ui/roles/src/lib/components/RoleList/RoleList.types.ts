export enum RoleStatus {
  active = 'Approved',
  pending = 'Pending',
  requested = 'Requested',
  rejected = 'Rejected',
  synced = 'Synced',
}

export enum ExpirationStatus {
  expired = 'Expired',
}
