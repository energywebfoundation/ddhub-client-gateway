import { ChannelMessageBoxList } from '@ddhub-client-gateway-frontend/ui/messaging';

export default function MessageInbox() {
  return (
    <main>
      <ChannelMessageBoxList />
    </main>
  );
}
